services:
  picpay-desafio:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - '9090:9090'
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
